# paper_code_of_QifanYang
for point clouds completion training, its in main_pcc.py. the mesh generation is in main.py
use produce_data to generate your owner date fot trainging and testing and change input path in main_pcc and data_pcc.
the extra extension is in the point_ops and manifold. 
you need to change the manifold path in main.py and network_p2m
